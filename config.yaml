arbitrage:
  min_profit_percent: 1.5   # umbral mínimo de ganancia

odds_api:
  key: ""                   # se cargará desde .env
  regions: eu
  markets: h2h,spreads,totals  # mercados a solicitar

notifications:
  telegram:
    bot_token: ""           # desde .env
    chat_id: ""             # desde .env